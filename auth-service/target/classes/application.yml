server:
  port: 8081

spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:http://localhost:8888 # ALTERADO

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # ALTERADO